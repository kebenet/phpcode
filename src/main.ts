// src/main.ts
import './code-editor-app.js'; // Use .js extension as Vite will handle TS to JS transpilation

// You can also import global styles here if necessary
// import './global-styles.css';

// Any other global setup or configuration can go here.
console.log("code-editor-app component loaded via main.ts");
